# BBB MP4 Night Queue Configuration

# BigBlueButton domain name (auto-detected during installation)
BBB_DOMAIN_NAME=bbb.example.com

# MP4 output location (where converted videos are stored)
COPY_TO_LOCATION=/var/www/bigbluebutton-default/recording

# Timezone for working hours calculation
# Examples: Asia/Yekaterinburg, Europe/Moscow, America/New_York, UTC
TIMEZONE=Asia/Yekaterinburg

# Working hours (24-hour format, in specified timezone)
# Night processing will run between these hours
WORK_START_HOUR=22
WORK_END_HOUR=7

# Maximum parallel conversions (recommended: 1-3)
MAX_PARALLEL_JOBS=2

# Conversion timeout in seconds (default: 2 hours)
CONVERSION_TIMEOUT=7200

# Directories (usually don't need to change)
BBB_MP4_DIR=/var/www/bbb-mp4
QUEUE_DIR=/var/www/bbb-mp4/queue
LOG_DIR=/var/www/bbb-mp4/logs
MP4_OUTPUT_DIR=/var/www/bigbluebutton-default/recording
PRESENTATION_DIR=/var/bigbluebutton/published/presentation

# Backup stop delay in minutes (how long to wait before force stop)
BACKUP_STOP_DELAY=15

# Queue check interval in seconds (how often to check for new jobs)
QUEUE_CHECK_INTERVAL=60

# Empty queue wait time in seconds (how long to wait when queue is empty)
EMPTY_QUEUE_WAIT=300
